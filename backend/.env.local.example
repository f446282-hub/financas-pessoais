# Configuração para desenvolvimento local com SQLite
# Copie este arquivo para .env e ajuste se necessário

# Banco de dados SQLite (não precisa instalar PostgreSQL!)
DATABASE_URL=sqlite:///./financas_pessoais.db

# Chave secreta para JWT (gere uma nova em produção!)
SECRET_KEY=local-development-key-change-in-production-12345

# Configurações de ambiente
DEBUG=true
ENVIRONMENT=development

# CORS (frontend rodando localmente)
CORS_ORIGINS=["http://localhost:3000"]

# Algoritmo JWT
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

